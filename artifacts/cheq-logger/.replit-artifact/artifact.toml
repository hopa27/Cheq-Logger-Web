kind = "web"
previewPath = "/"
title = "CHEQ Logger"
version = "1.0.0"
id = "artifacts/cheq-logger"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18424

[services.development]
run = "pnpm --filter @workspace/cheq-logger run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cheq-logger", "run", "build" ]
publicDir = "artifacts/cheq-logger/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18424"
BASE_PATH = "/"
